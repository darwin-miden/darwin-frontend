diff --git a/node_modules/@miden-sdk/use-miden-para-react/dist/index.js b/node_modules/@miden-sdk/use-miden-para-react/dist/index.js
index acdd65e..6136362 100644
--- a/node_modules/@miden-sdk/use-miden-para-react/dist/index.js
+++ b/node_modules/@miden-sdk/use-miden-para-react/dist/index.js
@@ -234,6 +234,15 @@ function ParaSignerProviderInner({
         if (!publicKey) throw new Error("Failed to get public key from wallet");
         const commitment = await (0, import_miden_para3.evmPkToCommitment)(publicKey);
         const commitmentBytes = commitment.serialize();
+        // PATCH(darwin): deterministic account seed derived from the (stable)
+        // pubkey commitment — see the .mjs build for the full rationale. Without
+        // it, initializeSignerAccount used a random seed => a different Miden
+        // account id per connect, stranding funds.
+        const __darwinSeedDigest = await crypto.subtle.digest(
+          "SHA-256",
+          commitmentBytes instanceof Uint8Array ? commitmentBytes : new Uint8Array(commitmentBytes)
+        );
+        const __darwinAccountSeed = new Uint8Array(__darwinSeedDigest);
         const signCallback = (0, import_miden_para2.signCb)(
           p,
           wallet,
@@ -246,7 +255,8 @@ function ParaSignerProviderInner({
             signCb: signCallback,
             accountConfig: {
               publicKeyCommitment: commitmentBytes,
-              storageMode: AccountStorageMode.public(),
+              storageMode: (typeof process !== "undefined" && process.env && process.env.NEXT_PUBLIC_PARA_PRIVATE === "1") ? AccountStorageMode.private() : AccountStorageMode.public(),
+              accountSeed: __darwinAccountSeed,
               ...(customComponents == null ? void 0 : customComponents.length) ? { customComponents } : {},
               ...importAccountId ? { importAccountId } : {}
             },
diff --git a/node_modules/@miden-sdk/use-miden-para-react/dist/index.mjs b/node_modules/@miden-sdk/use-miden-para-react/dist/index.mjs
index 3097255..2333ee6 100644
--- a/node_modules/@miden-sdk/use-miden-para-react/dist/index.mjs
+++ b/node_modules/@miden-sdk/use-miden-para-react/dist/index.mjs
@@ -216,6 +216,20 @@ function ParaSignerProviderInner({
         if (!publicKey) throw new Error("Failed to get public key from wallet");
         const commitment = await evmPkToCommitment(publicKey);
         const commitmentBytes = commitment.serialize();
+        // PATCH(darwin): deterministic account seed derived from the (stable)
+        // pubkey commitment. Upstream left accountSeed unset, so
+        // initializeSignerAccount fell back to a RANDOM 32-byte seed and derived
+        // a DIFFERENT Miden account id on every connect — stranding funds on a
+        // fresh account each reconnect. Hashing the stable commitment gives the
+        // same seed => same account id for the same Para wallet, across
+        // reconnects and store clears. The seed is only anti-collision salt for
+        // the id; security still comes from the Para MPC key, so deriving it from
+        // the public commitment is safe.
+        const __darwinSeedDigest = await crypto.subtle.digest(
+          "SHA-256",
+          commitmentBytes instanceof Uint8Array ? commitmentBytes : new Uint8Array(commitmentBytes)
+        );
+        const __darwinAccountSeed = new Uint8Array(__darwinSeedDigest);
         const signCallback = createSignCb(
           p,
           wallet,
@@ -229,6 +243,7 @@ function ParaSignerProviderInner({
             accountConfig: {
               publicKeyCommitment: commitmentBytes,
               storageMode: AccountStorageMode.public(),
+              accountSeed: __darwinAccountSeed,
               ...(customComponents == null ? void 0 : customComponents.length) ? { customComponents } : {},
               ...importAccountId ? { importAccountId } : {}
             },
